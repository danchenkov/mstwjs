<%= form_with(model: friend) do |form| %>
  <% if friend.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(friend.errors.count, "error") %> prohibited this friend from being saved:</h2>

      <ul>
        <% friend.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :request_sender_id, style: "display: block" %>
    <%= form.number_field :request_sender_id %>
  </div>

  <div>
    <%= form.label :request_receiver_id, style: "display: block" %>
    <%= form.number_field :request_receiver_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
